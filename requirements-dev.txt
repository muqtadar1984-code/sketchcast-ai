# Development-only dependencies (not deployed)
# Install locally with: pip install -r requirements-dev.txt
-r requirements.txt

# PDF extraction (local pre-processing only — not deployed)
docling>=2.0.0
easyocr>=1.7.0
