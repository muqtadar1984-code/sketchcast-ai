# Runtime dependencies for the math microservice ONLY.
# Deliberately minimal — this service must not drag in the worker's full
# requirements.txt (supabase, pymupdf, streamlit, ...).
fastapi>=0.110.0
uvicorn>=0.24.0
sympy>=1.12

# Dev/test only (not needed at runtime; installed by the repo's dev env):
# pytest>=7.0.0
# httpx>=0.25.0   # fastapi.testclient transport
