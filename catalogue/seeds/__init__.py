"""Curriculum seed files (``*.json``, one per curriculum) and their loader.

A seed file is ``{"curriculum": {...}, "nodes": [...]}``; see ``loader.py``
for the exact shape and the idempotent write order.
"""
